---
title: "Sea ice data"
format: 
  revealjs:
    slide-number: c/t
    code-fold: false
    chalkboard: true
    theme: white
    footer: "GEO4251 Climate Change & Storms"
bibliography: References.bib
editor: source
---


## Get the sea ice data

Download the sea level data using the `get_seaice()` function from the {hockeystick} package.

```{r}
#| echo: true
library(hockeystick)

seaice <- get_seaice(write_cache = TRUE)
```

##

Look at the data using the `head()` function.

```{r}
#| echo: true
seaice |>
  head()
 
```
Sea ice extent in millions of square kilometers every July 31 since 1979.

## Make a time-series plot

```{r}
#| echo: true
seaice |>
  plot_seaice()
```
